services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pottery
      POSTGRES_PASSWORD: pottery
      POSTGRES_DB: pottery
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
